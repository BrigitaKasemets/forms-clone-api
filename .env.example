# Environment Configuration

# JWT Secret - Generate a strong, random secret (use a secure random generator)
JWT_SECRET=your_strong_jwt_secret_here

# Database Configuration
DB_PATH=./forms.db

# Server Configuration
PORT=3000
