# Environment Configuration

# JWT Secret - Generate a strong, random secret (use a secure random generator)
JWT_SECRET=your_strong_jwt_secret_here

# Database Configuration
DB_PATH=./forms.db

# Server Configuration
PORT=3000

# CORS Configuration - Allow communication with frontend on port 3001
CORS_ORIGIN=http://localhost:3001